import 'package:flutter/material.dart';

const defaultPadding = 16.0;
const padding24 = 24.0;

class S {
  static h(double h) {
    return SizedBox(
      height: h,
    );
  }

  static w(double w) {
    return SizedBox(
      width: w,
    );
  }
}
